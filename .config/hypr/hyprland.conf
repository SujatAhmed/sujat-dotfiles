
# Please note not all available settings / options are set here.
# For a full list, see the wiki


#lt shi See https://wiki.hyprland.org/Configuring/Monitors/
# Without this, the resolution stays at 1280 * 720 for some reason

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch

exec-once =/home/sujat/.local/bin/optmization
exec-once =dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once =systemctl --user import-environment DISPLAY WAYLAND_DISPLAY XDG_CURRENT_DESKTOP



exec-once= waybar & hyprpaper
#exec-once= hyprctl dispatch exec "sleep 15s && qbittorrent"
monitor=,highres,auto,1
#The line below turn the night-light feature on
exec = hyprshade auto
exec-once = wl-paste --type text --watch cliphist store #Stores only text data

exec-once = wl-paste --type image --watch cliphist store #Stores only image data


# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Set programs that you use
$obsidian = obsidian --ozone-platform=wayland --enable-features=UseOzonePlatform,WaylandWindowDecorations
$terminal = alacritty
$fileManager = dolphin
$menu = wofi --show drun
$browser = MOZ_ENABLE_WAYLAND=1 firefox
$text-editor = ~/.config/scripts/launch-nvim.zsh
$email-client = thunderbird
$volume-up = pactl set-sink-volume @DEFAULT_SINK@ +3125
$volume-down = pactl set-sink-volume @DEFAULT_SINK@ -3125


# Some default env vars.
#env = XCURSOR_SIZE,24 #Already in the wrapper
env = QT_QPA_PLATFORMTHEME,qt6ct # change to qt6ct if you have that

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = caps:swapescape #swap caps lock and escape
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0.6 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 6
    gaps_out = 15
    border_size = 4
    #col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    
    col.active_border = rgba(ffffffee) rgba(ffffffee) 45deg;
    col.inactive_border = rgba(595959ee)

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5 
    
    blur {
        enabled = true
        size = 3
        passes = 1
    }

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = no

# Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    force_split = 2
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = false
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = 1 # Set to 0 or 1 to disable the anime mascot wallpapers
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
#device:epic-mouse-v1 {
#   sensitivity = -0.5
#}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
#windowrulev2 = nomaximizerequest, class:.* # You'll probably like this.


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, C, killactive, 
bind = $mainMod, M, exit, 
bind = $mainMod, V, togglefloating, 
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, T, togglesplit, # dwindle
bind = $mainMod, F, fullscreen, 1
bind = SUPER, Z, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy

#(for master)bind = $mainMod, W, layoutmsg, swapwithmaster #swaps the currently focused window with the master window
bind = , Print, exec, grim -g "$(slurp)" - | wl-copy #TODO figure out to take screenshots without the yellowing effects from hyprshade
bind = ALT, F, workspace, 1
bind = ALT, F, exec, $browser

bind = Alt, T,  workspace, 2
bind = ALT, T, exec, $terminal
bind = ALT SHIFT, T, exec, $terminal

bind = ALT, N, workspace, 3
bind = ALT, N, exec, $text-editor
bind = ALT SHIFT, N, exec, $text-editor


bind = ALT, O, workspace, 4
bind = ALT, O, exec, $obsidian

bind = ALT, B, workspace, 5
bind = ALT, B, exec, $email-client

bind = ALT, E, exec, $fileManager

bind = ALT, M, exec, $menu
bind = CTRL, up, exec, $volume-up
bind = CTRL, down, exec, $volume-down


# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d
bind = $mainMod SHIFT, H, swapwindow, l
bind = $mainMod SHIFT, L, swapwindow, r
bind = $mainMod SHIFT, K, swapwindow, u
bind = $mainMod SHIFT, J, swapwindow, d
bind = $mainMod CTRL, L, resizeactive, 40 0
bind = $mainMod CTRL, H, resizeactive, -40 0
bind = $mainMod CTRL, K, resizeactive, 0 -40 
bind = $mainMod CTRL, J, resizeactive, 0 40 



# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
